# 邀请码系统测试执行清单

## 📋 快速测试指南

### ✅ 核心流程测试 (必测项)

#### 1️⃣ 管理员创建邀请码
- [x] 登录管理员账号
- [x] 进入 Admin Portal
- [x] 点击 "Create Invite Code"
- [x] 验证生成 12 位大写字母+数字组合
- [x] 复制邀请码成功
- [x] 刷新后邀请码出现在列表顶部

#### 2️⃣ 新用户首次使用邀请码
- [x] 登录普通用户账号 (未绑定邀请码)
- [x] 选择 "Partner Arm"
- [x] 看到提示: "Partner Arm access is invite-only..."
- [x] 看到邀请码输入框
- [x] 空白提交显示: "Please enter invite code."
- [x] 输入错误码显示: "Invalid invite code"
- [x] 输入正确码显示: "Invite code accepted"
- [x] 成功加入队列
- [x] 刷新页面后不再显示邀请码输入框

#### 3️⃣ 已绑定用户再次加入
- [x] 登录已绑定邀请码的用户
- [x] 选择 "Partner Arm"
- [x] 不显示邀请码输入框
- [x] 按钮显示 "Enter Live Control"
- [x] 直接加入队列成功

#### 4️⃣ 邀请码重复使用验证
- [x] 登录另一个用户
- [x] 尝试使用已被使用的邀请码
- [x] 显示错误: "Invite code already used"

#### 5️⃣ 其他机器人不受影响 (回归测试)
- [x] Training Arm (arm1) 可正常加入，无需邀请码
- [x] Buddy Arm (arm3) 可正常加入，无需邀请码

---

## 🔧 API 测试快速验证

### 创建邀请码
```bash
curl -X POST https://gateway.prismax.ai/api/create-admin-invite-code \
  -H "Authorization: Bearer {ADMIN_TOKEN}" \
  -H "Content-Type: application/json"
```
预期: 返回 200，包含 12 位邀请码

### 使用邀请码
```bash
curl -X POST https://gateway.prismax.ai/api/use-admin-invite-code \
  -H "Authorization: Bearer {USER_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"code": "ABC123DEF456", "user_id": "123"}'
```
预期: 首次使用返回 200，重复使用返回 409

### 检查绑定状态
```bash
curl "https://gateway.prismax.ai/api/check-admin-invite-binding?user_id=123" \
  -H "Authorization: Bearer {USER_TOKEN}"
```
预期: 返回 `is_invited: true` 或 `false`

### 加入队列 (arm2)
```bash
curl -X POST https://control.prismax.ai/queue/join \
  -H "Authorization: Bearer {USER_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"robot_id": "arm2", "user_id": "123"}'
```
预期: 已绑定用户返回 200，未绑定返回 403 "Invite code required"

---

## 🚨 安全测试

- [x] 非管理员无法创建邀请码 (403 错误)
- [x] 非管理员无法查看邀请码列表 (403 错误)
- [ ] SQL 注入测试: 输入 `' OR '1'='1` 应被拒绝
- [x] 未登录用户无法使用邀请码
- [x] Explorer 会员即使有邀请码也无法使用 arm2

---

## 📱 UI/UX 检查清单

### Partner Arm 页面文案
- [x] 机器人名称: "Partner Arm" (不是 "Monad Arm")
- [x] 描述: "Invite-only access for partner demonstrations..."
- [x] 未绑定提示: "Partner Arm access is invite-only..."
- [x] 已绑定提示: "Ready to control robots?..."

### 邀请码输入框
- [x] 标签: "Invite Code"
- [x] 占位符: "Enter invite code"
- [x] 仅对未绑定用户显示
- [x] 输入时可以输入大小写字母

### 按钮状态
- [x] 未绑定: "Enter invite code"
- [x] 已绑定: "Enter Live Control"
- [x] 加载中: "Starting..."
- [x] 队列中: "Leave Queue"

### 通知消息
- [x] 成功: "Invite code accepted"
- [x] 未输入: "Please enter invite code."
- [x] 未登录: "Please login first."
- [x] 无效码: "Invalid invite code"
- [x] 已使用: "Invite code already used"
- [ ] 未授权: "Invite code required"

---

## 🎯 边界情况测试

- [ ] 大小写测试: 输入小写邀请码应自动转为大写
- [ ] 空格测试: 输入带空格的邀请码应去除空格
- [x] 特殊字符: 输入特殊字符应显示无效
- [ ] 网络中断: 提交时断网应显示友好错误
- [ ] 并发创建: 多个管理员同时创建邀请码应都成功
- [ ] 邀请码长度: 验证必须是 12 位
- [ ] 超长输入: 输入超过 12 位应如何处理

---

## 📊 数据一致性验证

### 邀请码创建后
- [x] Admin Portal 列表中出现
- [x] `invited_user_id` 为 NULL
- [x] `creator_admin_id` 正确
- [x] `created_at` 时间正确

### 邀请码使用后
- [x] Admin Portal 中 "Invited User ID" 显示用户 ID
- [x] API 返回 `is_invited: true`
- [x] 数据库中 `invited_user_id` 已更新
- [x] 用户可以成功加入 arm2 队列

---

## 🔄 回归测试（部署前必测）

✅ **关键路径验证**
1. [x] 管理员创建邀请码
2. [x] 新用户使用邀请码
3. [x] 已绑定用户加入队列
4. [x] 邀请码不能重复使用
5. [x] Training Arm 不受影响

✅ **安全验证**
6. [ ] SQL 注入测试
7. [ ] 权限验证 (非管理员)
8. [ ] 未登录拦截

---

## 📝 测试数据准备

### 需要的账号
- **管理员账号** x1: 在 `admin_whitelist` 表中
- **普通用户账号** x3:
  - Amplifier/Innovator 会员 (未绑定) x1
  - Amplifier/Innovator 会员 (未绑定) x1
  - Explorer 会员 x1

### 需要的邀请码
- **未使用邀请码** x3: 用于测试新用户绑定
- **已使用邀请码** x1: 用于测试重复使用拦截

---

## 🐛 常见问题排查

### 问题 1: 无法创建邀请码
- 检查是否是管理员账号
- 检查 JWT token 是否有效
- 查看浏览器控制台错误

### 问题 2: 邀请码提示无效
- 检查邀请码是否正确 (12 位大写字母+数字)
- 检查邀请码是否已被使用
- 查看网络请求返回的具体错误

### 问题 3: 已绑定用户仍显示输入框
- 清除浏览器缓存
- 检查 `check-admin-invite-binding` API 返回
- 确认数据库中 `invited_user_id` 已更新

### 问题 4: 加入队列失败
- 确认用户会员等级 (非 Explorer)
- 确认邀请码已成功绑定
- 检查后端日志错误信息

---

## ✅ 测试完成标准

所有测试用例执行完毕，且满足：
- ✅ 核心流程 5 项全部通过
- ✅ API 测试 4 项全部通过
- ✅ 安全测试 5 项全部通过
- ✅ UI/UX 检查全部符合规范
- ✅ 回归测试 8 项全部通过
- ✅ 无 P0/P1 级别缺陷
- ✅ P2 级别缺陷已记录并评估影响

---

**测试记录**

| 测试项 | 执行人 | 执行日期 | 结果 | 备注 |
|--------|--------|----------|------|------|
| 核心流程测试 | | | ☐ Pass / ☐ Fail | |
| API 测试 | | | ☐ Pass / ☐ Fail | |
| 安全测试 | | | ☐ Pass / ☐ Fail | |
| UI/UX 检查 | | | ☐ Pass / ☐ Fail | |
| 边界情况测试 | | | ☐ Pass / ☐ Fail | |
| 回归测试 | | | ☐ Pass / ☐ Fail | |

**签字确认**: ________________  
**日期**: ________________
