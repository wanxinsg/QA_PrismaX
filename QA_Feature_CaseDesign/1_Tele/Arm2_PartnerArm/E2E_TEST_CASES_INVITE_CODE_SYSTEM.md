# E2E 测试用例 - 邀请码系统

## 一、Commits 影响分析

### 1. Commit 1390b3b (Backend): added admin invited codes
**文件变更：**
- `app_prismax_user_management/app.py`

**功能影响：**
- ✅ 新增管理员邀请码生成功能
- ✅ 新增邀请码管理 API (创建、查询、使用)
- ✅ 添加了 localhost CORS 支持用于本地开发

**新增 API 端点：**
1. `POST /api/create-admin-invite-code` - 创建邀请码 (需要 JWT 认证)
2. `GET /api/get-admin-invite-codes` - 获取邀请码列表 (需要 JWT 认证)
3. `POST /api/use-admin-invite-code` - 使用邀请码绑定用户

---

### 2. Commit a80b2ad (Frontend): added admin invite codes
**文件变更：**
- `src/components/Admin/AdminInviteCodesTable.js` (新文件)
- `src/components/Admin/AdminPortal.js`

**功能影响：**
- ✅ 新增管理员邀请码管理界面
- ✅ 支持创建邀请码并复制到剪贴板
- ✅ 支持分页查看邀请码列表
- ✅ 显示邀请码状态（是否已被使用）

---

### 3. Commit cfa8962 (Backend): added invited user backend
**文件变更：**
- `app_prismax_tele_op_services/app.py`
- `app_prismax_user_management/app.py`

**功能影响：**
- ⚠️ **破坏性变更**: arm2 队列加入逻辑从 Monad 钱包验证改为邀请码验证
- ✅ 新增检查用户邀请码绑定状态的 API
- ⚠️ 未绑定邀请码的用户无法加入 arm2 队列

**新增 API 端点：**
- `GET /api/check-admin-invite-binding` - 检查用户是否已绑定邀请码

**关键逻辑变更：**
```python
# 旧逻辑: 检查 Monad 钱包
if robot_id == 'arm2':
    monad_address = (user_row[3] or "").strip()
    if not monad_address:
        # 检查 Monad 交易记录
        
# 新逻辑: 检查邀请码
if robot_id == 'arm2':
    invited = conn.execute(
        sqlalchemy.text("""
            SELECT 1 FROM admin_invite_codes
            WHERE invited_user_id = :uid
            LIMIT 1
        """),
        {"uid": user_id}
    ).fetchone()
    if not invited:
        return jsonify({"error": "Invite code required"}), 403
```

---

### 4. Commit c245cdc (Frontend): added partner arm
**文件变更：**
- `src/components/TeleOp/QueuePanel.module.css`
- `src/components/TeleOp/TeleOp.js`
- `src/components/TeleOp/TeleOpQueueBody.js`
- `src/components/TeleOp/TeleOpSelection.js`
- `src/components/header/ConnectWalletHeader.js`

**功能影响：**
- ✅ 品牌重命名: "Monad Arm" → "Partner Arm"
- ✅ 新增邀请码输入框 UI
- ✅ 加入队列前自动验证或绑定邀请码
- ✅ 移除 Monad 钱包相关 UI 元素
- ⚠️ 改变用户加入 arm2 队列的流程

**关键 UI 变更：**
- 邀请码输入框（仅对未绑定用户显示）
- 提示文本更新为邀请制说明
- 按钮文本动态显示（"Enter invite code" vs "Enter Live Control"）

---

## 二、系统架构影响

### 数据库表结构
```sql
-- admin_invite_codes 表（推测结构）
CREATE TABLE admin_invite_codes (
    invite_id / id              INT PRIMARY KEY AUTO_INCREMENT,
    code                        VARCHAR(12) UNIQUE NOT NULL,
    creator_admin_id / admin_id INT NOT NULL,
    invited_user_id             INT NULL,  -- NULL 表示未使用
    created_at / created_time   TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (creator_admin_id) REFERENCES admin_whitelist(admin_id),
    FOREIGN KEY (invited_user_id) REFERENCES users(userid)
);
```

### API 调用流程

#### 管理员创建邀请码流程：
```
Admin Portal UI → POST /api/create-admin-invite-code
                → 后端生成 12 位随机码
                → 插入数据库
                → 返回邀请码给管理员
                → 管理员复制并分享
```

#### 用户使用邀请码流程：
```
用户选择 Partner Arm
  ↓
GET /api/check-admin-invite-binding (检查是否已绑定)
  ↓
如果未绑定：
  - 显示邀请码输入框
  - 用户输入邀请码
  - 点击 "Enter invite code"
  ↓
POST /api/use-admin-invite-code (绑定邀请码)
  ↓
成功后：POST /queue/join (加入队列)
  ↓
后端验证 admin_invite_codes 表中有该用户记录
  ↓
允许加入队列或拒绝
```

---

## 三、风险与兼容性问题

### 1. ⚠️ 破坏性变更
- **arm2 访问权限变更**: 之前通过 Monad 钱包验证的用户现在无法访问
- **数据迁移需求**: 需要为现有 arm2 用户创建邀请码记录

### 2. 🔒 安全考虑
- 邀请码为 12 位大写字母+数字，理论上有 36^12 种组合，难以暴力破解
- 邀请码绑定后不可更改（一次性使用）
- 需要验证用户 token 才能绑定邀请码

### 3. 🐛 潜在 Bug
- 邀请码大小写敏感性（前端强制转大写，后端需确保一致）
- 并发创建邀请码可能产生冲突（已有重试机制，最多 5 次）
- 用户已在 arm2 队列中时切换账号的边界情况

---

## 四、手工 E2E 测试用例

### 测试环境准备
- **前端**: https://app.prismax.ai 或本地 localhost:3000
- **后端**: 确保后端服务运行且数据库已包含 `admin_invite_codes` 表
- **测试账号**:
  - 管理员账号 (有 admin_whitelist 记录)
  - 普通用户账号 A (未绑定邀请码)
  - 普通用户账号 B (未绑定邀请码)
  - 普通用户账号 C (已绑定邀请码 - 如果有)

---

### 测试套件 1: 管理员邀请码管理

#### TC-1.1: 管理员创建邀请码
**前置条件**: 已登录管理员账号

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 访问管理员门户页面 | 成功加载 Admin Portal |
| 2 | 定位到 "Admin Invite Codes" 表格区域 | 显示邀请码表格，包含 "Create Invite Code" 按钮 |
| 3 | 点击 "Create Invite Code" 按钮 | 弹出模态框，显示邀请码创建界面 |
| 4 | 点击模态框中的 "Create" 按钮 | 成功生成 12 位邀请码，显示在输入框中 |
| 5 | 验证邀请码格式 | 邀请码为 12 位大写字母+数字组合 |
| 6 | 点击 "Copy" 按钮 | 邀请码复制到剪贴板，粘贴验证成功 |
| 7 | 点击 "Close" 关闭模态框 | 模态框关闭 |
| 8 | 刷新表格或页面 | 新创建的邀请码出现在列表顶部 |
| 9 | 验证表格字段 | 显示 Invite ID, Code, Invited User ID (为 '--'), Creator Admin ID, Created At |

**测试数据**: 记录生成的邀请码用于后续测试

---

#### TC-1.2: 查看邀请码列表
**前置条件**: 数据库中已有多条邀请码记录

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 访问 Admin Portal | 显示邀请码表格 |
| 2 | 检查表格排序 | 邀请码按 Invite ID 或 Created At 倒序排列（最新在上） |
| 3 | 如果邀请码超过 20 条，检查分页 | 显示分页控件 (Previous, 1, 2, ..., Next) |
| 4 | 点击 "Next" 按钮 | 成功跳转到第 2 页 |
| 5 | 点击页码 "1" | 返回第 1 页 |
| 6 | 识别已使用的邀请码 | "Invited User ID" 列显示用户 ID（非 '--'） |
| 7 | 识别未使用的邀请码 | "Invited User ID" 列显示 '--' |

---

#### TC-1.3: 管理员权限验证
**前置条件**: 准备普通用户账号（非管理员）

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 使用普通用户账号登录 | 登录成功 |
| 2 | 尝试访问 Admin Portal 页面 | 无权限访问或看不到邀请码管理界面 |
| 3 | 使用普通用户 token 调用 API: `POST /api/create-admin-invite-code` | 返回 403 错误: "Admin not authorized" |
| 4 | 使用普通用户 token 调用 API: `GET /api/get-admin-invite-codes` | 返回 403 错误: "Admin not authorized" |

---

### 测试套件 2: 用户使用邀请码

#### TC-2.1: 新用户使用邀请码加入 Partner Arm 队列
**前置条件**: 
- 用户账号 A (未绑定邀请码)
- 有效的未使用邀请码 (从 TC-1.1 获取)

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 A | 登录成功 |
| 2 | 导航到 Tele-Op 页面 | 显示机器人选择界面 |
| 3 | 点击 "Partner Arm" 卡片 | 进入 Partner Arm 控制页面 |
| 4 | 检查队列面板 | 显示提示文本: "Partner Arm access is invite-only. Enter your invite code to join the queue." |
| 5 | 检查 UI 元素 | 显示邀请码输入框 (placeholder: "Enter invite code") |
| 6 | 检查按钮文本 | 显示 "Enter invite code" 按钮 |
| 7 | 不输入邀请码，直接点击按钮 | 显示警告通知: "Please enter invite code." |
| 8 | 输入错误的邀请码 (如 "WRONGCODE123") | 点击按钮后显示错误通知: "Invalid invite code" |
| 9 | 输入正确的邀请码 | 输入邀请码 (从 TC-1.1 复制) |
| 10 | 点击 "Enter invite code" 按钮 | 显示通知: "Invite code accepted"，按钮文本变为 "Starting..." |
| 11 | 等待加入队列 | 成功加入队列，按钮变为 "Leave Queue"，显示队列位置 |
| 12 | 检查邀请码输入框 | 邀请码输入框消失（已绑定用户不再显示） |
| 13 | 离开队列 | 点击 "Leave Queue" 按钮，成功离开 |
| 14 | 刷新页面后重新进入 Partner Arm | 不再显示邀请码输入框，按钮直接显示 "Enter Live Control" |

**验证点**: 
- 在管理员门户检查邀请码状态
- 该邀请码的 "Invited User ID" 列应显示用户 A 的 ID

---

#### TC-2.2: 已绑定邀请码的用户加入队列
**前置条件**: 用户账号 A (已完成 TC-2.1，已绑定邀请码)

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 A | 登录成功 |
| 2 | 导航到 Partner Arm 页面 | 进入 Partner Arm 控制页面 |
| 3 | 检查 UI | 不显示邀请码输入框 |
| 4 | 检查提示文本 | 显示: "Ready to control robots? Join queue for 5-minute sessions and earn Prisma Points." |
| 5 | 检查按钮文本 | 显示 "Enter Live Control" |
| 6 | 点击 "Enter Live Control" | 直接加入队列，无需再次输入邀请码 |
| 7 | 验证队列状态 | 显示队列位置或 "Session Active" |

---

#### TC-2.3: 邀请码重复使用验证
**前置条件**: 
- 用户账号 B (未绑定邀请码)
- 已被用户 A 使用的邀请码

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 B | 登录成功 |
| 2 | 导航到 Partner Arm 页面 | 显示邀请码输入框 |
| 3 | 输入已被用户 A 使用的邀请码 | 输入邀请码 |
| 4 | 点击 "Enter invite code" | 显示错误通知: "Invite code already used" |
| 5 | 验证用户 B 状态 | 未成功绑定，仍显示邀请码输入框 |

---

#### TC-2.4: 邀请码大小写敏感性测试
**前置条件**: 
- 用户账号 B (未绑定邀请码)
- 新的未使用邀请码 (如 "ABC123DEF456")

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 B | 登录成功 |
| 2 | 导航到 Partner Arm 页面 | 显示邀请码输入框 |
| 3 | 输入小写邀请码 "abc123def456" | 输入邀请码 |
| 4 | 点击 "Enter invite code" | 前端自动转换为大写，成功绑定 (显示 "Invite code accepted") |
| 5 | 验证绑定状态 | 成功加入队列 |

---

#### TC-2.5: 未登录用户访问 Partner Arm
**前置条件**: 未登录状态

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 访问应用首页（未登录） | 显示首页 |
| 2 | 导航到 Tele-Op 页面 | 显示机器人选择界面 |
| 3 | 点击 "Partner Arm" 卡片 | 进入 Partner Arm 页面 |
| 4 | 检查队列面板 | 显示邀请码输入框和按钮 |
| 5 | 输入邀请码并点击按钮 | 显示警告通知: "Please login first." |
| 6 | 不输入邀请码，直接点击按钮 | 显示警告通知: "Please login first." |

---

#### TC-2.6: Explorer 会员尝试加入 Partner Arm
**前置条件**: Explorer 等级用户账号

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录 Explorer 会员账号 | 登录成功，会员等级显示 "Explorer Member" |
| 2 | 导航到 Partner Arm 页面 | 显示邀请码输入框 |
| 3 | 输入有效邀请码 | 输入邀请码 |
| 4 | 点击 "Enter invite code" | 可能显示 "Invite code accepted" (如果邀请码验证在队列验证之前) |
| 5 | 尝试加入队列 | 显示升级会员模态框: "Explorer tier cannot use robots" |
| 6 | 验证 | 无法加入队列，需要升级会员等级 |

**注**: 根据代码逻辑，会员等级检查在邀请码检查之前，Explorer 会员应该在加入队列时被拦截，而不是在邀请码验证时。

---

### 测试套件 3: 其他机器人队列（回归测试）

#### TC-3.1: Training Arm (arm1) 队列加入
**前置条件**: 任意登录用户

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 | 登录成功 |
| 2 | 导航到 Training Arm 页面 | 进入 Training Arm 控制页面 |
| 3 | 检查 UI | 不显示邀请码输入框 |
| 4 | 点击 "Enter Live Control" | 直接加入队列，无需邀请码 |
| 5 | 验证队列功能 | 正常加入队列和控制机器人 |

---

#### TC-3.2: Buddy Arm (arm3) 队列加入
**前置条件**: 任意登录用户

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 | 登录成功 |
| 2 | 导航到 Buddy Arm 页面 | 进入 Buddy Arm 控制页面 |
| 3 | 检查 UI | 不显示邀请码输入框 |
| 4 | 点击 "Enter Live Control" | 加入队列（可能有使用次数限制） |
| 5 | 验证队列功能 | 正常加入队列和控制机器人 |

---

### 测试套件 4: API 端点测试

#### TC-4.1: 创建邀请码 API
**端点**: `POST /api/create-admin-invite-code`

**测试用例 4.1.1: 成功创建**
```bash
# 请求
curl -X POST https://gateway.prismax.ai/api/create-admin-invite-code \
  -H "Authorization: Bearer {ADMIN_JWT_TOKEN}" \
  -H "Content-Type: application/json"

# 预期响应 (200)
{
  "success": true,
  "data": {
    "invite_id": 123,
    "code": "A1B2C3D4E5F6",
    "invited_user_id": null,
    "creator_admin_id": 1,
    "created_at": "2026-01-14T12:00:00Z"
  }
}
```

**测试用例 4.1.2: 未授权访问**
```bash
# 请求 (无 token)
curl -X POST https://gateway.prismax.ai/api/create-admin-invite-code \
  -H "Content-Type: application/json"

# 预期响应 (401 或 403)
{
  "success": false,
  "msg": "Admin not authorized"
}
```

---

#### TC-4.2: 获取邀请码列表 API
**端点**: `GET /api/get-admin-invite-codes`

**测试用例 4.2.1: 成功获取**
```bash
# 请求
curl -X GET https://gateway.prismax.ai/api/get-admin-invite-codes \
  -H "Authorization: Bearer {ADMIN_JWT_TOKEN}"

# 预期响应 (200)
{
  "success": true,
  "data": [
    {
      "invite_id": 123,
      "code": "A1B2C3D4E5F6",
      "invited_user_id": 456,
      "creator_admin_id": 1,
      "created_at": "2026-01-14T12:00:00Z"
    },
    {
      "invite_id": 122,
      "code": "Z9Y8X7W6V5U4",
      "invited_user_id": null,
      "creator_admin_id": 1,
      "created_at": "2026-01-13T10:00:00Z"
    }
  ]
}
```

---

#### TC-4.3: 使用邀请码 API
**端点**: `POST /api/use-admin-invite-code`

**测试用例 4.3.1: 成功绑定**
```bash
# 请求
curl -X POST https://gateway.prismax.ai/api/use-admin-invite-code \
  -H "Authorization: Bearer {USER_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "code": "A1B2C3D4E5F6",
    "user_id": "456"
  }'

# 预期响应 (200)
{
  "success": true,
  "data": {
    "invite_id": 123,
    "code": "A1B2C3D4E5F6",
    "invited_user_id": 456,
    "creator_admin_id": 1,
    "created_at": "2026-01-14T12:00:00Z"
  }
}
```

**测试用例 4.3.2: 邀请码不存在**
```bash
# 请求
curl -X POST https://gateway.prismax.ai/api/use-admin-invite-code \
  -H "Authorization: Bearer {USER_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "code": "INVALIDCODE1",
    "user_id": "456"
  }'

# 预期响应 (404)
{
  "success": false,
  "msg": "Invalid invite code"
}
```

**测试用例 4.3.3: 邀请码已使用**
```bash
# 请求 (使用已绑定的邀请码)
curl -X POST https://gateway.prismax.ai/api/use-admin-invite-code \
  -H "Authorization: Bearer {USER_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "code": "A1B2C3D4E5F6",
    "user_id": "789"
  }'

# 预期响应 (409)
{
  "success": false,
  "msg": "Invite code already used"
}
```

---

#### TC-4.4: 检查邀请码绑定状态 API
**端点**: `GET /api/check-admin-invite-binding`

**测试用例 4.4.1: 已绑定用户**
```bash
# 请求
curl -X GET "https://gateway.prismax.ai/api/check-admin-invite-binding?user_id=456" \
  -H "Authorization: Bearer {USER_TOKEN}"

# 预期响应 (200)
{
  "success": true,
  "data": {
    "is_invited": true,
    "invite_id": 123,
    "code": "A1B2C3D4E5F6",
    "invited_user_id": 456,
    "created_at": "2026-01-14T12:00:00Z"
  }
}
```

**测试用例 4.4.2: 未绑定用户**
```bash
# 请求
curl -X GET "https://gateway.prismax.ai/api/check-admin-invite-binding?user_id=789" \
  -H "Authorization: Bearer {USER_TOKEN}"

# 预期响应 (200)
{
  "success": true,
  "data": {
    "is_invited": false
  }
}
```

---

#### TC-4.5: 加入队列 API (Partner Arm)
**端点**: `POST /queue/join`

**测试用例 4.5.1: 已绑定邀请码的用户加入队列**
```bash
# 请求
curl -X POST https://control.prismax.ai/queue/join \
  -H "Authorization: Bearer {USER_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "robot_id": "arm2",
    "user_id": "456"
  }'

# 预期响应 (200)
{
  "success": true,
  "position": 1,
  "estimated_wait_time": 300
}
```

**测试用例 4.5.2: 未绑定邀请码的用户尝试加入队列**
```bash
# 请求
curl -X POST https://control.prismax.ai/queue/join \
  -H "Authorization: Bearer {USER_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "robot_id": "arm2",
    "user_id": "789"
  }'

# 预期响应 (403)
{
  "error": "Invite code required"
}
```

---

### 测试套件 5: 边界情况和异常测试

#### TC-5.1: 并发创建邀请码
**前置条件**: 管理员账号

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开两个浏览器窗口，登录同一管理员账号 | 登录成功 |
| 2 | 在两个窗口中同时点击 "Create Invite Code" | 两个窗口都显示创建模态框 |
| 3 | 在两个窗口中同时点击 "Create" | 两个窗口都成功创建邀请码 |
| 4 | 验证生成的邀请码 | 两个邀请码不同（唯一性约束） |

---

#### TC-5.2: 网络中断时使用邀请码
**前置条件**: 用户账号 (未绑定邀请码)

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 | 登录成功 |
| 2 | 导航到 Partner Arm 页面 | 显示邀请码输入框 |
| 3 | 输入有效邀请码 | 输入邀请码 |
| 4 | 断开网络连接 | 网络断开 |
| 5 | 点击 "Enter invite code" | 显示错误通知: "Failed to verify invite code" 或网络错误 |
| 6 | 恢复网络连接 | 网络恢复 |
| 7 | 再次点击 "Enter invite code" | 成功绑定邀请码 |

---

#### TC-5.3: 用户在队列中时更换账号
**前置条件**: 
- 用户账号 A (已绑定邀请码，在 Partner Arm 队列中)
- 用户账号 B (已绑定邀请码)

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 A | 登录成功 |
| 2 | 加入 Partner Arm 队列 | 成功加入队列 |
| 3 | 不离开队列，直接退出登录 | 登出成功 |
| 4 | 登录用户账号 B | 登录成功 |
| 5 | 尝试加入 Partner Arm 队列 | 应该能正常加入（用户 A 的会话应已失效） |

**验证点**: 后端应清理用户 A 的队列状态

---

#### TC-5.4: 邀请码输入特殊字符
**前置条件**: 用户账号 (未绑定邀请码)

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 | 登录成功 |
| 2 | 导航到 Partner Arm 页面 | 显示邀请码输入框 |
| 3 | 输入包含特殊字符的字符串 (如 "ABC@#$123!") | 输入字符串 |
| 4 | 点击 "Enter invite code" | 显示错误通知: "Invalid invite code" |
| 5 | 输入包含空格的字符串 (如 "ABC 123 DEF") | 输入字符串 |
| 6 | 点击 "Enter invite code" | 前端应去除空格，后端返回 "Invalid invite code" (如果不存在) |

---

#### TC-5.5: SQL 注入测试
**前置条件**: 用户账号 (未绑定邀请码)

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 | 登录成功 |
| 2 | 导航到 Partner Arm 页面 | 显示邀请码输入框 |
| 3 | 输入 SQL 注入字符串: `' OR '1'='1` | 输入字符串 |
| 4 | 点击 "Enter invite code" | 显示 "Invalid invite code"，不应该绕过验证 |
| 5 | 验证数据库 | 数据库没有被篡改，无异常记录 |

**安全验证**: 使用参数化查询，应防止 SQL 注入

---

### 测试套件 6: UI/UX 测试

#### TC-6.1: 响应式设计测试
**前置条件**: 用户账号 (未绑定邀请码)

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在桌面浏览器上访问 Partner Arm 页面 | UI 正常显示，邀请码输入框宽度合适 |
| 2 | 调整浏览器窗口大小到移动设备尺寸 (375px) | UI 自适应，邀请码输入框仍然可用 |
| 3 | 在移动设备上访问 Partner Arm 页面 | UI 正常显示，触摸操作正常 |
| 4 | 输入邀请码并提交 | 移动端键盘弹出，输入正常 |

---

#### TC-6.2: 加载状态和禁用状态测试
**前置条件**: 用户账号 (未绑定邀请码)

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录用户账号 | 登录成功 |
| 2 | 导航到 Partner Arm 页面 | 显示邀请码输入框 |
| 3 | 输入有效邀请码 | 输入邀请码 |
| 4 | 点击 "Enter invite code" | 按钮文本变为 "Starting..."，按钮禁用 |
| 5 | 检查输入框状态 | 输入框在请求期间被禁用 |
| 6 | 等待请求完成 | 按钮恢复可用状态（或进入队列） |

---

#### TC-6.3: 文案和提示信息检查

| 测试点 | 页面/组件 | 预期文案 |
|--------|-----------|----------|
| 机器人名称 | TeleOpSelection | "Partner Arm" (不是 "Monad Arm") |
| 机器人描述 | TeleOpSelection | "Invite-only access for partner demonstrations and special sessions." |
| 未绑定时提示 | TeleOpQueueBody | "Partner Arm access is invite-only. Enter your invite code to join the queue." |
| 已绑定时提示 | TeleOpQueueBody | "Ready to control robots? Join queue for 5-minute sessions and earn Prisma Points." |
| 未绑定时按钮 | TeleOpQueueBody | "Enter invite code" |
| 已绑定时按钮 | TeleOpQueueBody | "Enter Live Control" |
| 加载中按钮 | TeleOpQueueBody | "Starting..." |
| 队列中按钮 | TeleOpQueueBody | "Leave Queue" |
| 邀请码输入框标签 | QueuePanel | "Invite Code" |
| 邀请码输入框占位符 | QueuePanel | "Enter invite code" |
| 成功通知 | TeleOpQueueBody | "Invite code accepted" |
| 错误通知 - 未输入 | TeleOpQueueBody | "Please enter invite code." |
| 错误通知 - 无效码 | TeleOpQueueBody | "Invalid invite code" |
| 错误通知 - 已使用 | TeleOpQueueBody | "Invite code already used" |
| 错误通知 - 未授权 | TeleOpQueueBody | "Invite code required" (来自后端) |

---

### 测试套件 7: 数据一致性测试

#### TC-7.1: 邀请码绑定后数据验证
**前置条件**: 完成 TC-2.1 (用户 A 已绑定邀请码)

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 管理员门户查看邀请码列表 | 找到用户 A 使用的邀请码 |
| 2 | 检查 "Invited User ID" 列 | 显示用户 A 的 user_id (如 456) |
| 3 | 检查 "Created At" 时间 | 时间早于当前时间 |
| 4 | 调用 API: `GET /api/check-admin-invite-binding?user_id=456` | 返回 `is_invited: true` 和邀请码信息 |
| 5 | 数据库直接查询 | `SELECT * FROM admin_invite_codes WHERE invited_user_id = 456` 返回对应记录 |

---

#### TC-7.2: 邀请码创建后数据验证

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 管理员创建邀请码 | 成功创建，记录返回的 invite_id |
| 2 | 刷新邀请码列表 | 新邀请码出现在列表中 |
| 3 | 数据库直接查询 | `SELECT * FROM admin_invite_codes WHERE invite_id = {id}` 返回记录 |
| 4 | 验证字段值 | code 为 12 位大写字母数字，invited_user_id 为 NULL，creator_admin_id 正确 |

---

## 五、测试执行建议

### 优先级排序
1. **P0 (关键路径)**: TC-1.1, TC-2.1, TC-2.2, TC-2.3, TC-4.5
2. **P1 (重要功能)**: TC-1.2, TC-2.4, TC-2.5, TC-2.6, TC-3.1, TC-4.3
3. **P2 (边界情况)**: TC-5.1, TC-5.2, TC-5.3, TC-5.4, TC-5.5
4. **P3 (UI/UX)**: TC-6.1, TC-6.2, TC-6.3
5. **P4 (数据一致性)**: TC-7.1, TC-7.2

### 回归测试清单
每次部署前必须执行的测试：
- ✅ TC-2.1: 新用户使用邀请码
- ✅ TC-2.2: 已绑定用户加入队列
- ✅ TC-2.3: 邀请码重复使用验证
- ✅ TC-3.1: Training Arm 队列加入 (确保未影响其他机器人)
- ✅ TC-5.5: SQL 注入测试 (安全验证)

### 性能测试建议
- 邀请码生成性能: 连续创建 100 个邀请码，测试响应时间
- 邀请码查询性能: 在数据库中有 10,000+ 邀请码时查询列表，测试分页性能
- 并发绑定测试: 100 个用户同时使用不同邀请码，测试数据库锁和并发处理

---

## 六、已知问题和改进建议

### 已知问题
1. **邀请码格式限制**: 目前只支持 12 位大写字母+数字，缺乏自定义长度功能
2. **无邀请码过期机制**: 邀请码一旦创建，永久有效，无法设置过期时间
3. **无邀请码撤销功能**: 管理员无法撤销或删除已创建的邀请码
4. **无使用统计**: 缺乏邀请码使用情况的统计和分析功能

### 改进建议
1. **邀请码批量生成**: 支持一次性生成多个邀请码
2. **邀请码过期时间**: 添加过期时间字段和自动过期机制
3. **邀请码备注**: 允许管理员为邀请码添加备注（如用途、接收人等）
4. **使用日志**: 记录邀请码使用时间、用户信息等详细日志
5. **导出功能**: 支持导出邀请码列表为 CSV 或 Excel
6. **搜索和筛选**: 支持按邀请码、用户 ID、状态等筛选

---

## 七、测试报告模板

### 测试执行记录表

| 测试用例 ID | 测试用例名称 | 执行日期 | 测试人员 | 执行结果 | 缺陷 ID | 备注 |
|-------------|-------------|----------|----------|----------|---------|------|
| TC-1.1 | 管理员创建邀请码 | 2026-01-15 | Tester A | ✅ Pass | - | - |
| TC-2.1 | 新用户使用邀请码 | 2026-01-15 | Tester A | ❌ Fail | BUG-001 | 大小写敏感问题 |
| ... | ... | ... | ... | ... | ... | ... |

### 缺陷报告模板

**缺陷 ID**: BUG-001  
**标题**: 邀请码大小写敏感导致验证失败  
**严重程度**: Medium  
**优先级**: P1  
**发现于**: TC-2.4  
**描述**: 用户输入小写邀请码时，前端未自动转换为大写，导致后端验证失败  
**复现步骤**:
1. 登录用户账号
2. 导航到 Partner Arm 页面
3. 输入小写邀请码 "abc123def456"
4. 点击 "Enter invite code"

**预期结果**: 自动转换为大写并成功验证  
**实际结果**: 显示 "Invalid invite code"  
**环境**: Chrome 120, macOS 14.0  
**附件**: screenshot.png

---

## 八、测试环境配置

### 前端环境
- **开发环境**: http://localhost:3000
- **测试环境**: https://beta-app.prismax.ai
- **生产环境**: https://app.prismax.ai

### 后端环境
- **网关 API**: https://gateway.prismax.ai
- **控制 API**: https://control.prismax.ai (PRISMAX_BACKEND_Control_URL)

### 测试数据准备
1. **管理员账号**: 至少 1 个有效的管理员账号（在 admin_whitelist 表中）
2. **普通用户账号**: 至少 3 个普通用户账号（不同会员等级）
   - Amplifier 或 Innovator 会员 x2
   - Explorer 会员 x1
3. **邀请码**: 至少 5 个未使用的邀请码

---

**文档版本**: 1.0  
**创建日期**: 2026-01-15  
**最后更新**: 2026-01-15  
**维护人员**: QA Team
